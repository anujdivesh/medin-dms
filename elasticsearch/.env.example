# Copy to .env and fill in real values. Do NOT commit .env.
# Read automatically by Docker Compose when run from this directory
# (docker-compose -f docker-compose-local.yml up -d).

ELASTIC_PASSWORD=
