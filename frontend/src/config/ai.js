// Default Hugging Face config (used only if you call chatWithContext)
// Leave token empty if not using the HF route.
export const HF_CONFIG = {
  token: '',
  model: 'meta-llama/Meta-Llama-3-8B-Instruct',
  maxNewTokens: 512,
}


